# SPEC-09 local HTTP verification

Both frozen packages matched direct Python inference on invented requests. 30,000-row label and probability workloads completed. Zero fitting or test scoring. Protected inputs unchanged. Research only; no production recommendation or SLA certification.

See verification.json for actual checks, package pins, hardware, timings and aggregate counters.
